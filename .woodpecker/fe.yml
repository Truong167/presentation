uses: .woodpecker/deploy.yml
environment: fe
with:
  validate_command: echo "Validating HTML files..." && test -f index.html && echo "index.html OK" || exit 1

when:
  event: manual
  evaluate: type == "fe"
